language: java
install: true

dist: precise
 
sudo: required

before_install:
 - chmod +x ./gradlew
 
before_script:
  - chmod +x gradlew
  - echo "We can put normal shell commands here"
  
jdk:
  - oraclejdk8



script:
    - gradle jenkinsBuild -x test
