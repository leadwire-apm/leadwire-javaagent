language: java
install: true

dist: precise
 
sudo: required

before_install:
 - chmod +x ./gradlew
 
before_script:
  - chmod +x gradlew
  - echo "We can put normal shell commands here"
  
jdk:
  - oraclejdk8


deploy:
  provider: releases
  api_key: "dae9c32bd86d6f629a0bc05e03e8e151fc6d0e17"
  file: "leadwire-1.13-SNAPSHOT-aspectj.jar"
  skip_cleanup: true
  on:
    tags: true


script:
    - gradle jenkinsBuild -x test
